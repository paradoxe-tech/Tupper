{
  "name": "tupper",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm run build",
    "server": "cd server && npm run dev",
    "start": "npm-run-all --parallel client server"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "luxon": "^3.5.0"
  }
}